name: phx

services:
  phoenix:
    image: arizephoenix/phoenix:latest
    restart: no
    ports:
      - "6006:6006" # Phoenix UI
      - "4317:4317" # OTLP gRPC
      - "4318:4318" # OTLP HTTP
    volumes:
      - phx_data:/root/.phoenix

volumes:
  phx_data:
